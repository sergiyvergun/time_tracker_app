import 'package:flutter/material.dart';

class JobsListTile extends StatelessWidget {
  const JobsListTile({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
